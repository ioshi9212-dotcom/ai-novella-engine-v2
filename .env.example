# ai-novella-engine-v2 environment

# Railway Volume mount path.
# Recommended Railway volume mount path: /data
NOVELLA_RUNTIME_DATA_ROOT=/data

# Railway usually provides this automatically when a volume is mounted.
# The app should use it as fallback if NOVELLA_RUNTIME_DATA_ROOT is not set.
# RAILWAY_VOLUME_MOUNT_PATH=/data
